#usda 1.0
(
    defaultPrim = "Material"
    doc = "Stub material resolving ConveyorBelt_A06.usd's missing Plastic_Rough_Black_A reference."
)

def Material "Material"
{
    token outputs:surface.connect = </Material/Shader.outputs:surface>

    def Shader "Shader"
    {
        uniform token info:id = "UsdPreviewSurface"
        color3f inputs:diffuseColor = (0.04, 0.04, 0.05)
        float inputs:metallic = 0.0
        float inputs:roughness = 0.85
        token outputs:surface
    }
}
